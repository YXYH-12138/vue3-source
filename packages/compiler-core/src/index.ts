/**
 * 1. 用来将模板字符串解析为模板 AST 的解析器 parser
 * 2. 用来将模板 AST 转换为 JavaScript AST 的转换器 transformer
 * 3. 用来根据 JavaScript AST 生成渲染函数代码的生成器 generator
 */

export function parser(template: string, options: any) {
	//
}
